# Deny direct access to sensitive files
<FilesMatch "^(db|mail_config|logger|rate_limit|session_guard)\.php$">
    Require all denied
</FilesMatch>

# Deny access to dot-files
<FilesMatch "^\.">
    Require all denied
</FilesMatch>
